function maxBanana(x,y,z,k) {
    x =
}

function bananaLeft(x,y) {
    // y is the total distance and since for efficency the trip has to be divided into 3
    var y2 = y/3;
    var a = tripNo(x)*y2;
    var left = x-a;
    return left;
}

function tripNo(x) {
    var u = 2*(x/1000)-1;
    document.write("the trip no is: "+u);
}

function noOfBananas() {
    var x = getElementById().value();


}